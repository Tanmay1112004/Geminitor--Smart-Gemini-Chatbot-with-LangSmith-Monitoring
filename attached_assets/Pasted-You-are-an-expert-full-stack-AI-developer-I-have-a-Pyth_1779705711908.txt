You are an expert full-stack AI developer. I have a Python LLM backend called 
"Geminitor Pro" using LangChain + Gemini 1.5 Flash + LangSmith. I want to 
completely replace the Streamlit frontend with a stunning production-grade 
HTML/CSS/JavaScript frontend connected to a FastAPI Python backend.

=== ARCHITECTURE ===
- Backend: FastAPI (Python) — handles all AI logic, LangChain, Gemini, RAG, LangSmith
- Frontend: Pure HTML + CSS + Vanilla JavaScript (NO React, NO framework)
- Communication: REST API (fetch/axios) + Server-Sent Events for streaming
- Deployment: Render (backend) + Render Static Site or GitHub Pages (frontend)

=== FRONTEND DESIGN (HTML/CSS/JS) ===
Design a ChatGPT-level dark themed UI with:

LAYOUT:
- Left sidebar (260px) + main chat area (flex layout)
- Sidebar: logo "🤖 Geminitor Pro", New Chat button, chat history list, 
  settings section at bottom
- Main area: header bar, chat messages container, input area at bottom
- Fully responsive (mobile hamburger menu for sidebar)

DARK THEME COLORS:
- Background: #0d0d0d
- Sidebar: #171717  
- Chat area: #212121
- Input box: #2f2f2f
- User bubble: #2f2f2f (right aligned)
- Bot bubble: transparent with left border accent #10a37f (ChatGPT green)
- Accent color: #10a37f
- Text: #ececec
- Subtext: #8e8ea0

CHAT UI FEATURES:
- Animated typing dots indicator (three bouncing dots while bot responds)
- Streaming text effect (characters appear one by one like ChatGPT)
- Message timestamps below each bubble
- Copy button (📋) on hover for each bot message
- User avatar (colored circle with initial) + Bot avatar (🤖 icon)
- Auto scroll to bottom on new message
- Empty state: centered logo + 4 suggestion cards 
  ("Explain machine learning", "Write Python code", "Summarize a document", 
  "Help me study")
- Smooth fade-in animation for each new message

SIDEBAR FEATURES:
- New Chat button (green, full width)
- Chat history: clickable previous conversation titles
- Settings panel: Model dropdown, Temperature slider, Max tokens slider, 
  Persona selector
- Each setting saves to localStorage
- Bottom: GitHub link, version badge "v2.0"

TOP HEADER:
- Current model name shown (e.g. "gemini-1.5-flash")
- Export button (📥) → downloads chat as .txt
- Clear chat button (🗑️)
- Dark/light mode toggle (🌙/☀️)

INPUT AREA:
- Floating textarea (auto-resize, max 5 lines)
- Send button (arrow icon, activates on Enter key)
- Attach file button (📎) for PDF/image upload
- Character counter
- "Geminitor Pro can make mistakes." disclaimer text below

ANALYTICS MODAL:
- Trigger via sidebar "📊 Analytics" button
- Modal popup showing:
  - Total messages (this session)
  - Average response time
  - Tokens used (simple bar visualization in pure CSS)
  - Most recent topics list

=== BACKEND (FastAPI) ===
Generate complete FastAPI backend:

ENDPOINTS:
POST /api/chat → send message, get response (with memory)
POST /api/chat/stream → streaming response via SSE
POST /api/upload/pdf → upload PDF for RAG
POST /api/upload/image → upload image for vision
GET /api/history → get chat history
DELETE /api/history → clear chat
GET /api/analytics → get session analytics
POST /api/export → export chat as PDF (fpdf2)
GET /health → health check for Render

BACKEND FEATURES:
- ConversationBufferWindowMemory (last 10 turns)
- RAG with FAISS + sentence-transformers for uploaded PDFs
- Gemini Vision for image understanding
- LangSmith tracing on every request
- Feedback endpoint (POST /api/feedback) for thumbs up/down
- CORS enabled for frontend domain
- Rate limiting (slowapi) — 20 requests/minute
- Request/response logging with response time tracking
- Error handling with proper HTTP status codes
- Environment variables via python-dotenv

=== PROJECT STRUCTURE ===
geminitor_pro/
├── backend/
│ ├── main.py # FastAPI app, all routes
│ ├── modules/
│ │ ├── chat_engine.py # LangChain + Gemini + Memory
│ │ ├── rag_module.py # FAISS + PDF processing
│ │ ├── vision_module.py # Gemini Vision
│ │ ├── analytics_module.py # Session tracking
│ │ └── export_module.py # PDF export
│ ├── requirements.txt
│ ├── Procfile
│ ├── render.yaml
│ └── .env.example
│
├── frontend/
│ ├── index.html # Main HTML (single page)
│ ├── style.css # All CSS (dark theme, animations)
│ ├── app.js # All JavaScript logic
│ ├── config.js # API base URL config
│ └── assets/
│ └── favicon.ico
│
└── README.md

=== JAVASCRIPT REQUIREMENTS ===
- NO jQuery, NO React — pure Vanilla JS (ES6+)
- Fetch API for all backend calls
- EventSource API for SSE streaming
- localStorage for settings persistence  
- Marked.js CDN for rendering markdown in bot responses
  (code blocks, bold, lists must render properly)
- Highlight.js CDN for code syntax highlighting in responses
- No other external JS libraries

=== DEPLOYMENT ===
Backend on Render (Web Service):
- Procfile: web: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
- render.yaml with envVars: GOOGLE_API_KEY, LANGCHAIN_API_KEY, 
  LANGCHAIN_TRACING_V2, LANGCHAIN_PROJECT

Frontend on Render (Static Site) or GitHub Pages:
- config.js exports BACKEND_URL pointing to Render backend URL
- All fetch calls use this config

=== CODE RULES ===
- Zero placeholders — every file 100% complete and working
- API keys only via .env, never hardcoded
- Clean commented code with docstrings on all functions
- Mobile first CSS with breakpoints at 768px and 480px
- All error states handled in JS (network error, API error, loading states)
- Backend returns consistent JSON: { success: bool, data: {}, error: string }

Generate ALL files in this order:
1. backend/main.py
2. backend/modules/chat_engine.py
3. backend/modules/rag_module.py  
4. backend/modules/vision_module.py
5. backend/modules/analytics_module.py
6. backend/modules/export_module.py
7. frontend/index.html
8. frontend/style.css
9. frontend/app.js
10. frontend/config.js
11. backend/requirements.txt
12. backend/Procfile
13. backend/render.yaml
14. backend/.env.example
15. README.md